PORT=8080
MUSICGEN_UPSTREAM=http://127.0.0.1:8101
SPLEETER_UPSTREAM=http://127.0.0.1:8102
MAGENTART_UPSTREAM=http://127.0.0.1:8103

# ngrok (gateway notebook): on by default; set USE_NGROK=0 for local-only
USE_NGROK=1
NGROK_AUTHTOKEN=
# Optional reserved domain on paid ngrok (otherwise omit)
# NGROK_DOMAIN=your-name.ngrok-free.app
